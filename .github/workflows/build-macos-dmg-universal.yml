name: macOS .dmg universal

run-name: ${{ github.ref_name }} macOS .dmg universal

on:
  push:
    tags:
      - '[0-9]*'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build-macos-dmg:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Checkout rwkv_mobile_flutter dependency
        uses: actions/checkout@v4
        with:
          repository: MollySophia/rwkv_mobile_flutter
          ref: dev
          path: rwkv_mobile_flutter
          fetch-depth: 1

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.5'
          channel: 'stable'
          cache: true

      - name: Override dependency path
        run: |
          echo "dependency_overrides:" > pubspec_overrides.yaml
          echo "  rwkv_mobile_flutter:" >> pubspec_overrides.yaml
          echo "    path: ./rwkv_mobile_flutter" >> pubspec_overrides.yaml

      - name: Install dependencies
        run: flutter pub get

      - name: Verify Flutter setup
        run: flutter doctor -v

      - name: Build macOS app (Universal)
        run: flutter build macos --release

      - name: 📦 Create dmg
        id: create-dmg
        run: |
          echo "::group::📦 Creating dmg"

          # 1. 提取版本号
          VERSION=$(grep "^version:" pubspec.yaml | sed 's/version: //' | sed 's/+.*//')
          BUILD_NUMBER=$(grep "^version:" pubspec.yaml | sed 's/.*+//')
          if [ -z "$BUILD_NUMBER" ]; then BUILD_NUMBER="1"; fi

          # 命名格式: rwkv_chat_1.0.0_1_macos-universal.dmg
          DMG_NAME="rwkv_chat_${VERSION}_${BUILD_NUMBER}_macos-universal.dmg"

          cd build/macos/Build/Products/Release

          # 2. 创建 dmg (使用提取出的名字)
          hdiutil create -volname "RWKV_Chat" -srcfolder "ci_demo.app" -ov -format UDZO "$DMG_NAME"

          DMG_PATH="$(pwd)/$DMG_NAME"

          echo "dmg_path=$DMG_PATH" >> $GITHUB_OUTPUT
          echo "dmg_name=$DMG_NAME" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT

          echo "✅ dmg created: $DMG_NAME"
          echo "::endgroup::"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          generate_release_notes: false
          # 👇 确保这里使用的是 steps.create-dmg.outputs.dmg_path
          files: ${{ steps.create-dmg.outputs.dmg_path }}

      - name: 🛠️ Install Tencent COSCMD
        if: startsWith(github.ref, 'refs/tags/')
        run: pip install coscmd

      - name: 📤 Upload to Tencent COS
        if: startsWith(github.ref, 'refs/tags/')
        env:
          COS_SECRET_ID: ${{ secrets.COS_SECRET_ID }}
          COS_SECRET_KEY: ${{ secrets.COS_SECRET_KEY }}
          COS_BUCKET: ${{ secrets.COS_BUCKET }}
          COS_REGION: ${{ secrets.COS_REGION }}
          LOCAL_FILE: ${{ steps.create-dmg.outputs.dmg_path }}
          REMOTE_FOLDER: releases/${{ steps.create-dmg.outputs.version }}/
        run: |
          echo "::group::📤 Uploading to Tencent COS"
          # 配置 coscmd
          coscmd config -a "$COS_SECRET_ID" -s "$COS_SECRET_KEY" -b "$COS_BUCKET" -r "$COS_REGION"
          # 执行上传
          echo "Uploading $LOCAL_FILE to $REMOTE_FOLDER"
          coscmd upload "$LOCAL_FILE" "$REMOTE_FOLDER"
          echo "✅ Upload completed"
          echo "::endgroup::"
